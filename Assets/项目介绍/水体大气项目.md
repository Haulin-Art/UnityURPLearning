# 水体大气项目技术文档

## 0. 核心项目文件路径

本项目的所有核心文件位于 `Assets` 目录下，以下是各系统核心文件的相对路径：

### 0.1 水体渲染系统文件

#### 主Shader文件
- `Assets/m_Shader/m_WaterShader/water.shader` - 水体主着色器

#### 库文件（Module）
- `Assets/m_Shader/m_WaterShader/ff_WaterCommon.hlsl` - 水体渲染基础工具库
- `Assets/m_Shader/m_WaterShader/ff_WaterFresnel.hlsl` - 水体菲涅尔效果模块
- `Assets/m_Shader/m_WaterShader/ff_WaterRefraction.hlsl` - 水体折射效果模块
- `Assets/m_Shader/m_WaterShader/ff_WaterReflection.hlsl` - 水体反射效果模块
- `Assets/m_Shader/m_WaterShader/ff_WaterSpecular.hlsl` - 水体高光计算模块
- `Assets/m_Shader/m_WaterShader/ff_WaterPhase.hlsl` - 水体相位函数模块
- `Assets/m_Shader/m_WaterShader/ff_WaterBSDF.hlsl` - 水体BSDF散射模块
- `Assets/m_Shader/m_WaterShader/ff_WaterRayMarching.hlsl` - 光线步进体积散射模块
- `Assets/m_Shader/m_WaterShader/ff_WaterForwardScatter.hlsl` - 前向散射计算模块
- `Assets/m_Shader/m_WaterShader/ff_WaterWave.hlsl` - 水面波浪模块
- `Assets/m_Shader/m_WaterShader/BSDF/BSDF_SSS.hlsl` - 次表面散射模块

#### 平面反射Renderer Feature
- `Assets/m_RendererFeature/PlanarReflection/PlanarReflectionRF.cs` - 平面反射渲染特性
- `Assets/m_RendererFeature/PlanarReflection/PlanarReflections.cs` - 平面反射核心逻辑
- `Assets/m_RendererFeature/PlanarReflection/PlanarReflectionTest.shader` - 测试用着色器

#### 屏幕MipMap处理Renderer Feature
- `Assets/m_RendererFeature/m_ScreenMipMap/ScreenMipMapRendererFeature.cs` - 屏幕MipMap渲染特性
- `Assets/m_RendererFeature/m_ScreenMipMap/ScreenMipMapPass.cs` - 屏幕MipMap渲染Pass
- `Assets/m_RendererFeature/m_ScreenMipMap/ScreenMipMapPreProcess.shader` - 预处理着色器
- `Assets/m_RendererFeature/m_ScreenMipMap/ScreenMipMapUsageExample.shader` - 使用示例着色器

#### 吃水线Renderer Feature
- `Assets/m_RendererFeature/WaterLine/Scripts/WaterLineRF.cs` - 吃水线渲染特性
- `Assets/m_RendererFeature/WaterLine/Scripts/control_WaterLineRenderer.cs` - 吃水线渲染器控制
- `Assets/m_RendererFeature/WaterLine/Scripts/cess.cs` - 辅助脚本
- `Assets/m_RendererFeature/WaterLine/Scripts/WaterLineRenderer.cs` - 吃水线渲染器
- `Assets/m_RendererFeature/WaterLine/Shader/ScreenWSPos.shader` - 屏幕空间世界位置着色器

#### 浅水方程系统
- `Assets/m_Effect/WaveEquation/WaveEquationControl.cs` - 浅水方程控制器
- `Assets/m_Effect/WaveEquation/WaveEquationCS.compute` - 浅水方程Compute Shader
- `Assets/m_Effect/WaveEquation/NewUnlitShader.shader` - 调试用着色器

### 0.2 大气渲染系统文件

#### 大气散射全景图生成
- `Assets/m_RendererFeature/MieScattering/Compute/AtmosScatteringPanoramaGenerator.cs` - 大气散射全景图生成器
- `Assets/m_RendererFeature/MieScattering/Compute/AtmosScatteringPanorama.compute` - 大气散射Compute Shader

#### 体积云Renderer Feature
- `Assets/m_RendererFeature/MieScattering/AtmosphereSystem/Materials/RendererFeature_Version/Adv_Cloud_RF_Version.cs` - 体积云渲染特性
- `Assets/m_RendererFeature/MieScattering/AtmosphereSystem/Materials/RendererFeature_Version/AdvancedVolumetricCloud.shader` - 体积云着色器
- `Assets/m_RendererFeature/MieScattering/AtmosphereSystem/Materials/RendererFeature_Version/CompsiteCloudAndAtoms.shader` - 云与大气散射合成着色器

### 0.3 项目文档
- `Assets/项目介绍/水体大气项目.md` - 本文档

---

## 1. 项目概述

本项目是一个综合性的水体和大气渲染系统，包含两个主要部分：**水体渲染系统**和**大气层体积云的大气散射系统**。通过多个子系统的协同工作，实现了高质量的实时水体和大气效果。

### 1.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                 水体大气系统                                   │
├─────────────────────────────────┬───────────────────────────────────────────────┤
│         水体渲染系统            │              大气渲染系统                    │
├─────────┬─────────┬─────────┬──┼───────────────┬───────────────────────────────┤
│ 基础水面│ 平面反射│ 屏幕MipMap│  │ 大气散射     │ 体积云                         │
│ Shader  │ 系统    │ 处理系统  │  │ 全景图生成   │ 渲染系统                       │
├─────────┼─────────┼─────────┼──┼───────────────┼───────────────────────────────┤
│water.shader│Planar │Screen   │  │AtmosScattering│Adv_Cloud_RF_Version.cs      │
│ff_Water*.  │Reflection│MipMap │  │PanoramaGenerator│AdvancedVolumetricCloud.   │
│hlsl       │RF.cs   │Pass.cs │  │.cs            │shader                       │
│           │        │       │  │AtmosScattering│CompsiteCloudAndAtoms.        │
│           │        │       │  │Panorama.      │shader                       │
│           │        │       │  │compute        │                              │
└─────────┴─────────┴─────────┴──┴───────────────┴───────────────────────────────┘
```

### 1.2 系统间关系

- **大气散射系统**：生成全景大气散射图，供水体Shader和Skybox采样
- **体积云系统**：使用大气散射图进行渲染，实现与大气层的视觉一致性
- **平面反射系统**：生成场景中不透明物体的平面反射纹理，供水体Shader使用
- **屏幕MipMap处理系统**：生成带有Mipmap层级的降采样屏幕纹理，实现水的散射模糊效果和焦散效果
- **吃水线系统**：生成吃水线和水下效果，增强水体与场景物体的交互感
- **浅水方程系统**：生成水面涟漪纹理，供水体Shader和焦散处理Shader使用

---

## 2. 水体渲染系统

### 2.1 水体Shader架构详解

水体Shader采用模块化设计，由一个主Shader和多个功能库文件组成，通过 `#include` 指令组织成完整的水体渲染系统。

#### 2.1.1 主Shader文件结构

**water.shader** 是水体渲染的主着色器文件，采用Unity的URP渲染管线。文件结构如下：

```hlsl
Shader "FluidFlux/water"
{
    // ==================== 属性定义 ====================
    Properties
    {
        // 基础颜色
        _MainTex ("纹理", 2D) = "white" {}
        _WaterCol ("浅水颜色",Color) = (0.0,0.3,0.6)
        _DeepWaterCol ("深水颜色",Color) = (0.0,0.3,0.6)

        // 3D水面置换纹理（法线+高度）
        _3DDisMap ("3D水面置换纹理", 3D) = "white" {}
        _SurfScale01 ("水面置换强度1", Range(-1.0,1.0)) = 1.0
        _SurfScale02 ("水面置换强度2", Range(-1.0,1.0)) = 0.5
        _SurfSpeed ("水面置换速度", Range(-5.0,5.0)) = 1.0
        _SurfNorScale ("水面法线强度", Range(0.0,5.0)) = 1.0

        // 矢量置换纹理（近岸海浪）
        _VectorDisMap ("矢量置换纹理", 2D) = "white" {}
        _VectorDisMapNormal("矢量置换纹理法线", 2D) = "white" {}
        _VectorDisMapScale ("矢量置换整体强度", Float) = 3.0
        _VDMXScale ("矢量置换x强度", Range(-2,2)) = 1.0
        _VDMYScale ("矢量置换y强度", Range(-2,2)) = -0.2

        // 海陆SDF与梯度
        _SDFTex ("SDF距离图", 2D) = "white" {}
        _GradientTex ("梯度图", 2D) = "white" {}

        // 菲涅尔设置
        _FresnelF0 ("菲涅尔F0", Range(0.0, 0.1)) = 0.02
        _FresnelPower ("菲涅尔强度", Range(1.0, 10.0)) = 5.0

        // 折射设置
        _RefractionStrength ("折射强度", Range(0.0, 0.1)) = 0.02
        _AbsorptionColor ("折射吸收颜色", Color) = (0.1, 0.2, 0.3)
        _RefractionBlurStart ("折射模糊开始深度", Range(0.0, 10.0)) = 0.0
        _RefractionBlurEnd ("折射模糊结束深度", Range(0.1, 50.0)) = 2.0
        _RefractionBlurStrength ("折射模糊强度", Range(0.0,10.0)) = 2.0

        // 反射设置
        _EnvReflectionStrength ("环境反射强度", Range(0.0, 2.0)) = 0.5
        _Roughness ("粗糙度", Range(0.0, 1.0)) = 0.3

        // 高光设置
        _SpecularPower ("高光锐度", Range(8.0, 256.0)) = 64.0
        _SpecularIntensity ("高光强度", Range(0.0, 3.0)) = 1.0

        // BSDF设置
        _ScatterColor ("散射颜色", Color) = (0.2, 0.5, 0.8)
        _BSDFAbsorptionColor ("BSDF吸收颜色", Color) = (0.1, 0.2, 0.3)
        _PhaseG ("相位参数G", Range(-1.0, 1.0)) = 0.8
        _Thickness ("厚度", Range(0.0, 1.0)) = 0.5
        _DepthScale ("深度缩放", Range(0.1, 50.0)) = 10.0

        // 光线步进设置
        [Toggle(_USE_RAY_MARCHING)] _UseRayMarching ("启用光线步进", Float) = 0
        _RayMarchSteps ("步进次数", Range(1, 16)) = 8
        _RayMarchIntensity ("步进强度", Range(0.0, 3.0)) = 1.0
        _RayMarchMaxDistance ("最大步进距离", Range(0.1, 50.0)) = 20.0

        // 次表面散射设置
        [Toggle(_USE_SSS)] _UseSSS ("启用次表面散射", Float) = 1
        _SSSColor ("次表面散射颜色", Color) = (0.0, 0.5, 0.8, 1.0)
        _SSSStrength ("次表面散射强度", Range(0.0, 5.0)) = 1.0
        _SSSDepthScale ("次表面散射深度缩放", Range(0.1, 50.0)) = 5.0
        _SSSFade ("次表面散射衰减", Color) = (1.0, 1.0, 1.0, 1.0)
        _SunGlitterIntensity ("太阳闪烁强度", Range(0.0, 2.0)) = 0.3
        _SunGlitterSpeed ("太阳闪烁速度", Range(0.0, 10.0)) = 2.0
    }
}
```

#### 2.1.2 库文件依赖关系

```
water.shader (主着色器)
    │
    ├── ff_WaterCommon.hlsl (必须，最基础的工具函数)
    │       ├── FF_PI 常量定义
    │       ├── FFLuminance 亮度计算
    │       ├── FFPow5 快速5次方
    │       ├── FFRemap 值重映射
    │       ├── FFBlendNormal 法线混合
    │       ├── FFSampleBlueNoise 蓝噪声采样
    │       └── 深度缓冲相关函数
    │
    ├── ff_WaterFresnel.hlsl (菲涅尔计算)
    │       ├── FFFresnelSchlick Schlick菲涅尔近似
    │       ├── FFFresnelWater 水体菲涅尔
    │       ├── FFFresnelEntry/Exit 入射/出射透射率
    │       └── FFIorToF0/FFF0ToIor 折射率转换
    │
    ├── ff_WaterRefraction.hlsl (折射效果)
    │       └── FFGetRefractionOffset 折射偏移计算
    │
    ├── ff_WaterReflection.hlsl (反射效果)
    │       └── FFSampleEnvReflection 环境反射采样
    │
    ├── ff_WaterSpecular.hlsl (高光计算)
    │       ├── FFCalculateSpecularBlinnPhong Blinn-Phong高光
    │       └── FFCalculateSpecularGGXSimple GGX高光
    │
    ├── ff_WaterPhase.hlsl (相位函数)
    │       ├── FFWaterPhaseFunctionFast 快速相位函数
    │       └── FFPhaseWaterBacklitFast 背光相位函数
    │
    ├── ff_WaterBSDF.hlsl (BSDF散射核心)
    │       ├── FFWaterBSDFInput/Output 结构体
    │       ├── FFComputeIncidentGeometry 入射几何计算
    │       ├── FFComputeThinLayerSSS 薄层次表面散射
    │       ├── FFComputeBacklitTransmission 背光透射
    │       └── FFEvaluateWaterBSDFSimple BSDF主评估
    │
    ├── ff_WaterRayMarching.hlsl (光线步进)
    │       ├── FFRayMarchConfig 步进配置
    │       ├── FFGetExponentialStepPosition 指数步进位置
    │       ├── FFSampleShadowAtPosition 阴影采样
    │       ├── FFRayMarchVolumeScattering 指数步进散射
    │       └── FFFastVolumeScattering 快速近似散射
    │
    └── ff_WaterForwardScatter.hlsl (前向散射)
            └── FFComputeWaterSSSFull 完整SSS计算
```

### 2.2 水体渲染核心原理

#### 2.2.1 水面几何构建

水体Shader使用多种技术构建水面几何：

**1. 矢量置换图近岸海浪**

通过SDF（符号距离场）纹理确定海岸线位置，结合梯度纹理计算海岸线的法线方向，然后使用矢量置换图在近岸区域生成动态的海浪效果：

```hlsl
// 核心置换计算
float3 decodeDisp(float3 disp)
{
    disp = pow(disp, 0.45);
    disp -= 0.5;
    disp -= float3(_VDMXScale, _VDMYScale, 0.5);
    disp = float3(disp.x, 0.0, disp.y);
    disp *= _VectorDisMapScale;
    disp += float3(_VDMXOffset, 0.0, _VDMYOffset);
    return disp;
}

// 浪花UV动画
float2 animUV(float2 uv, float sinBase, float time, float4 uv_ST)
{
    float w = 0.01;
    float2 newUV = uv * uv_ST.xy + uv_ST.zw;
    float nu = frac(newUV.x - time * _Speed);
    nu += -_sineScale * sin(((sinBase - time * _Speed) * 2.0 - 1.0) * PI * _SineCount);
    nu = clamp(frac(nu), w, 1.0 - w);
    float nv02m = pow(abs(frac(newUV.y) - 0.5), _WaveScale);
    float nv01m = pow(abs(frac(newUV.y) - 0.5), 1);
    float nv01 = clamp(((uv.x + _WavePos) * _WaveAngle - nv01m), w, 1.0 - w);
    float nv02 = clamp(((uv.x + _WavePos * 1.5) * _WaveAngle - nv02m), w, 1.0 - w);
    float nv = lerp(nv01, nv02, smoothstep(0.05, 0.3, nv01m));
    newUV = float2(nu, -nv);
    return newUV;
}
```

**2. 3D噪声水面起伏**

使用3D纹理存储Voronoi分形的海面高度和法线信息，通过时间偏移实现动态效果：

```hlsl
// 3D纹理采样水面起伏
float4 waterSurf = SAMPLE_TEXTURE3D_LOD(_3DDisMap, sampler_3DDisMap,
    float3(v.uv, frac(_Time.y * _SurfSpeed)), 0.0);

// 法线计算
float waveSurY = waterSurfU.x * _SurfScale01 - waterSurf.x * _SurfScale01;
float waveSurX = waterSurfR.x * _SurfScale01 - waterSurf.x * _SurfScale01;
float3 surfNor;
surfNor.x = waveSurX * _SurfNorScale * 10.0;
surfNor.y = waveSurY * _SurfNorScale * 50.0;
surfNor.z = sqrt(1 - pow(surfNor.x, 2) - pow(surfNor.y, 2));
```

#### 2.2.2 水面着色流程

**顶点着色器 (vert)**

1. 计算世界坐标和UV
2. 采样SDF和梯度纹理，确定海岸线位置
3. 计算矢量置换后的世界位置偏移
4. 计算TBN矩阵（切线、副切线、法线）
5. 采样3D噪声计算水面起伏
6. 组合置换后的新位置，输出到裁剪空间

**片元着色器 (frag)**

```hlsl
float4 frag(v2f i) : SV_Target
{
    // 1. 重新计算水面法线（更精确）
    float3 WaveNor = SAMPLE_TEXTURE3D(_3DDisMap, sampler_3DDisMap,
        float3(i.uv, frac(_Time.y * _SurfSpeed))).xyz;
    float3 Nor = float3((WaveNor.y * 2.0 - 1.0) * _SurfNorScale,
                        (WaveNor.z * 2.0 - 1.0) * _SurfNorScale, 0.0);
    Nor.z = sqrt(1 - pow(Nor.x, 2) - pow(Nor.y, 2));
    float3x3 waveTBN = float3x3(i.tanWave, i.bitWave, i.norWS);
    Nor = mul(Nor, waveTBN);
    Nor = normalize(float3(Nor.z, Nor.y, -Nor.x));

    // 2. 获取基础数据
    float3 viewDirWS = normalize(i.viewDirWS);
    float3 normal = Nor;
    float3 dispCol = SAMPLE_TEXTURE2D(_VectorDisMap, sampler_VectorDisMap, newUV).rgb;
    float sdf = SAMPLE_TEXTURE2D(_SDFTex, sampler_SDFTex, worldUV).x;
    float foamM = smoothstep(0.2, 0.0, -sdf);

    // 3. 折射效果
    float2 refractionOffset = FFGetRefractionOffset(normal, viewDirWS, _RefractionStrength);
    float2 refractedScreenUV = screenUV + refractionOffset;

    // 4. 基于水深选择Mipmap层级实现模糊
    float mipmapDepRamp = smoothstep(_RefractionBlurStart, _RefractionBlurEnd, waterDepth);
    float3 mipmapScreenColor = SAMPLE_TEXTURE2D_LOD(_ScreenMipMapRT,
        sampler_ScreenMipMapRT, refractedScreenUV, mipmapDepRamp * _RefractionBlurStrength).rgb;
    float3 refractionColor = mipmapScreenColor;

    // 5. 应用水体吸收效果
    refractionColor = FFApplyWaterAbsorption(refractionColor, waterDepth, _AbsorptionColor);
    float3 albedo = lerp(_WaterCol, _DeepWaterCol, rampMask);

    // 6. BSDF水体散射
    float thickness = saturate(waterDepth / _DepthScale);
    float3 extinctionCoeff = _ScatterColor + _BSDFAbsorptionColor;
    float3 scatterAlbedo = _ScatterColor / max(extinctionCoeff, 1e-6);

    #ifdef _USE_RAY_MARCHING
        FFRayMarchConfig rmConfig = FFCreateDefaultRayMarchConfig();
        rmConfig.stepCount = (int)_RayMarchSteps;
        rmConfig.maxDistance = min(waterDepth, _RayMarchMaxDistance);
        bsdfScattering = FFRayMarchVolumeScattering(...);
    #else
        bsdfScattering = FFEvaluateWaterScattering(...);
    #endif

    // 7. 高光计算
    float3 specCol = FFCalculateSpecularGGXSimple(normal, viewDirWS, lightDir, lightColor,
        _Roughness, _SpecularIntensity);

    // 8. 环境反射
    float3 reflectDir = reflect(-viewDirWS, normal);
    float3 envReflection = FFSampleEnvReflection(reflectDir, _Roughness, _EnvReflectionStrength);
    float fresnel = FFFresnelWater(normal, viewDirWS, _FresnelF0);
    float3 reflectionColor = FFBlendReflection(albedo, envReflection, fresnel, 1.0);

    // 9. 最终合成
    float3 finalColor = reflectionColor + lerp(float3(0, 0, 0), bsdfScattering, rampMask);
    finalColor += specCol;

    // 10. SSS效果
    #ifdef _USE_SSS
        float3 sss = FFComputeWaterSSSFull(...);
        float3 sunGlitter = FFComputeSunGlitterAnimated(...);
        finalColor += sss + sunGlitter;
    #endif

    return float4(finalColor + foam * 4.0, alpha);
}
```

### 2.3 水体BSDF模块详解

#### 2.3.1 BSDF双通道模型

水体BSDF采用双通道散射模型，将水体散射分为两个独立通道：

```
输出 = (diffR + diffT) * T_exit

其中：
diffR = 宏观体积散射 = G_entry * T_entry * S_volume
diffT = 薄层散射 = thinLayerSSS + backlitTransmission
```

#### 2.3.2 入射几何项

根据光线与法线的夹角，将入射光分配到不同的散射通道：

```hlsl
struct FFIncidentGeometry
{
    float G_entry;    // 正面入射：saturate(N·L)
    float G_sss;      // 侧面入射：1 - G_entry
    float G_backlit;  // 背面入射：saturate(-N·L)
    float T_entry;    // 入射菲涅尔透射率
};
```

#### 2.3.3 光程计算与非线性修正

对于薄水层（如波峰、浪花），使用非线性光程修正：

```hlsl
float FFComputeEffectivePathLength(float thickness, float3 scatterCoeff, float pathScale)
{
    // 线性光程
    float linearPath = thickness * pathScale;
    // 非线性光程：厚度平方项使薄层效果增强
    float nonlinearPath = thickness * thickness * pathScale *
        (1.0 + FFLuminance(scatterCoeff));
    // 根据光学深度在两者间插值
    float opticalDepth = FFLuminance(scatterCoeff) * thickness * pathScale;
    float strengthFactor = FF_SSS_NONLINEAR_STRENGTH * opticalDepth;
    float effectivePath = lerp(linearPath, nonlinearPath, saturate(strengthFactor));
    return effectivePath;
}
```

### 2.4 光线步进模块详解

#### 2.4.1 指数步进算法

光线步进模块使用指数步进代替均匀步进，使采样点在近处密集、远处稀疏：

```hlsl
// 指数分布位置计算
float FFGetExponentialStepPosition(float t, float k, float n)
{
    float denom = exp(k) - 1.0;
    return (exp(k * t) - 1.0) / denom;
}

// 步进循环
for (int i = 0; i < config.stepCount; i++)
{
    float t = (float(i) + dither) / float(config.stepCount);
    float normalizedDistance = FFGetExponentialStepPosition(t, config.expFactor, config.stepCount);
    float currentDistance = normalizedDistance * maxDistance;
    float3 currentWorldPos = rayOrigin + rayDirection * currentDistance;

    // 计算阴影、透射率、散射贡献并累积
    ...
}
```

#### 2.4.2 逐点阴影采样

可选的逐点阴影采样使水下阴影更加精确：

```hlsl
if (config.usePerStepShadow)
{
    currentShadow = 1.0 - FFSampleShadowAtPositionFast(currentWorldPos);
}
```

---

## 3. 大气渲染系统

### 3.1 大气层散射系统

#### 3.1.1 实现原理

大气层散射系统使用Compute Shader计算大气散射效果，并生成全景大气散射图。主要原理包括：

- **瑞利散射**：模拟空气中分子对蓝光的散射，使天空呈现蓝色
- **米氏散射**：模拟大气中尘埃、水滴等粒子的散射，产生光晕效果
- **臭氧吸收**：模拟臭氧层对特定波长光的吸收，影响日落时的颜色
- **光线步进**：通过数值积分计算光线在大气中的散射和吸收

#### 3.1.2 核心代码

**AtmosScatteringPanoramaGenerator.cs**：

```csharp
// 生成大气散射全景图
public void Generate()
{
    // 检查必要资源
    if (outputTexture == null || atmosComputeShader == null)
    {
        Debug.LogError("必要资源未设置！");
        return;
    }

    // 检查输出纹理格式
    if (!outputTexture.enableRandomWrite)
    {
        Debug.LogWarning("输出纹理未启用随机写入，正在自动启用...");
        outputTexture.enableRandomWrite = true;
    }

    // 获取kernel索引
    if (kernelIndex < 0)
    {
        kernelIndex = atmosComputeShader.FindKernel("CSMain");
    }

    // 设置Compute Shader参数
    SetComputeShaderParameters();

    // 计算线程组数量
    int threadGroupsX = Mathf.CeilToInt((float)outputTexture.width / 8f);
    int threadGroupsY = Mathf.CeilToInt((float)outputTexture.height / 8f);

    // 执行Compute Shader
    atmosComputeShader.Dispatch(kernelIndex, threadGroupsX, threadGroupsY, 1);
}
```

**AtmosScatteringPanorama.compute**：

```hlsl
// 核心大气散射计算
float3 ComputeAtmosScattering(float3 rayOrigin, float3 rayDir, float rayLength,
                              float3 planetCenter, float planetRadius, float atmosHeight,
                              float3 sunDir, int numSamples, int numLightSamples,
                              out float3 outViewTransmittance)
{
    float ds = rayLength / (float)numSamples;
    float3 p = rayOrigin + rayDir * ds * 0.5;
    
    float3 accumRayleigh = float3(0, 0, 0);
    float3 accumMie = float3(0, 0, 0);
    float3 accumSunMie = float3(0, 0, 0);
    
    float3 totalRayleighDepth = float3(0, 0, 0);
    float3 totalMieDepth = float3(0, 0, 0);
    float3 totalOzoneDepth = float3(0, 0, 0);

    float3 betaR = _ScatterScale.x * kBetaR;
    float3 betaM = _ScatterScale.y * kBetaM;
    float mieExtinction = _MieExtinction > 0 ? _MieExtinction : 1.1 * _ScatterScale.y * 21e-6;

    // 计算相位函数
    float cosTheta = dot(rayDir, sunDir);
    float phaseR = RayleighPhase(cosTheta);
    float phaseM = MiePhase(cosTheta, _MieG);
    float sunPhaseM = MiePhase(cosTheta, _SunMieG);

    for (int i = 0; i < numSamples; i++)
    {
        float h = length(p - planetCenter) - planetRadius;
        
        if (h < 0.0)
            break;
        
        float rayleighDensity = GetRayleighDensity(h);
        float mieDensity = GetMieDensity(h);
        float ozoneDensity = GetOzoneDensity(h);

        // 计算从当前点到大气层边界的光线长度
        float2 lightInter = RaySphereIntersect(p, sunDir, planetCenter, planetRadius + atmosHeight);
        float lightRayLength = max(0.0, lightInter.y);
        
        // 检测地球遮挡
        float2 planetInter = RaySphereIntersect(p, sunDir, planetCenter, planetRadius);
        bool sunBlocked = false;
        if (planetInter.x > 0.0)
        {
            if (planetInter.x < lightRayLength)
            {
                sunBlocked = true;
            }
        }

        if (lightRayLength > 0.0 && !sunBlocked)
        {
            // 计算光线方向的光学深度
            float3 lightOpticalDepth = ComputeOpticalDepth(p, sunDir, lightRayLength, numLightSamples,
                                                           planetCenter, planetRadius);
            
            // 视线方向到当前点的光学深度
            float3 viewOpticalDepth = float3(
                totalRayleighDepth.x + rayleighDensity * ds * 0.5,
                totalMieDepth.y + mieDensity * ds * 0.5,
                totalOzoneDepth.z + ozoneDensity * ds * 0.5
            );
            
            // 总光学深度
            float3 totalOpticalDepth = (viewOpticalDepth + lightOpticalDepth) * _AtmosIntensity;
            
            // 计算透射率 T = exp(-τ)
            float3 tau = betaR * totalOpticalDepth.x 
                       + mieExtinction * totalOpticalDepth.y 
                       + kOzone * totalOpticalDepth.z;
            float3 transmittance = exp(-tau);
            
            // 累积散射光
            accumRayleigh += rayleighDensity * ds * betaR * phaseR * transmittance;
            accumMie += mieDensity * ds * betaM * phaseM * transmittance;
            
            // 累积额外的太阳Mie散射
            accumSunMie += _SunMieIntensity * mieDensity * ds * betaM * sunPhaseM * transmittance;
        }

        // 更新累积光学深度
        totalRayleighDepth.x += rayleighDensity * ds;
        totalMieDepth.y += mieDensity * ds;
        totalOzoneDepth.z += ozoneDensity * ds;
        
        p += rayDir * ds;
    }

    // 计算视线方向的透射率
    float3 viewTau = betaR * totalRayleighDepth.x 
                   + mieExtinction * totalMieDepth.y 
                   + kOzone * totalOzoneDepth.z;
    outViewTransmittance = exp(-viewTau);
    
    // 返回所有散射的总和
    return accumRayleigh + accumMie + accumSunMie;
}
```

### 3.2 体积云系统

#### 3.2.1 实现原理

体积云系统使用光线步进算法渲染体积云，与大气层散射系统紧密集成。主要特点：

- **上下半球对称**：避免为水面反射生成额外的体积云纹理
- **3D噪声纹理**：使用3D噪声生成云的形状和细节
- **多重散射**：模拟光在云中的多次散射，增强真实感
- **风动画**：通过噪声纹理的偏移实现云的移动效果

#### 3.2.2 核心代码

**Adv_Cloud_RF_Version.cs**：

```csharp
public override void Execute(ScriptableRenderContext context, ref RenderingData renderingData)
{
    if (renderingData.cameraData.camera.cameraType == CameraType.Preview)
        return;
    if (m_Settings.AdvanceCloudRFVersionMat == null) return;

    CommandBuffer cmd = CommandBufferPool.Get(m_Settings.passName);

    RenderTargetIdentifier cameraColorTarget = renderingData.cameraData.renderer.cameraColorTargetHandle;

    cmd.Blit(null, _AtmosRFCloudTex, m_Settings.AdvanceCloudRFVersionMat);

    cmd.SetGlobalTexture("_AtmosRFCloudTex", _AtmosRFCloudTex);

    context.ExecuteCommandBuffer(cmd);
    CommandBufferPool.Release(cmd);
}
```

**AdvancedVolumetricCloud.shader**：

```hlsl
// 计算云密度（核心函数）
float CalculateCloudDensity(float3 WorldPos, float sunHeight, CloudLayerParams CloudParams)
{
    // 计算相对高度
    float Height = length(WorldPos - CloudParams.Center) - CloudParams.InnerRadius;
    float NormHeight = Height * CloudParams.ToNormHeight;
    
    // 高度裁剪
    if (NormHeight < 0.0 || NormHeight > 1.0)
        return 0.0;
    
    // 应用高度渐变
    float HeightFactor = SAMPLE_TEXTURE2D_LOD(_CloudHeightGradient, sampler_CloudHeightGradient, 
                                            float2(NormHeight, 0.5), 0).r;

    // 风动画
    float3 WindOffset = _WindDirection * _WindSpeed * _Time.y * _TimeScale;
    
    // 采样3D噪声纹理
    float3 TexCoord = (WorldPos + WindOffset * 100 + float3(0, 300 * _Time.y, 0)) * _CloudDensityScale;
    float3 TexCoord_detail = (WorldPos + WindOffset * 100) * _CloudDensityScale;
    float4 Noise3D = SAMPLE_TEXTURE3D_LOD(_CloudTex, sampler_CloudTex,
        TexCoord.xzy * _CloudAxisScale / (20.0 * _CloudTiling), 0);
    float4 NoiseDetail = SAMPLE_TEXTURE3D_LOD(_CloudDetailTex, sampler_CloudDetailTex,
        TexCoord_detail / (2.5 * _CloudDetailTiling), 0);
    
    // 组合噪声通道
    float Density = Noise3D.r + Noise3D.g * 0.5 + Noise3D.b * 0.2 + Noise3D.a * 0.1;
    Density /= 1.8;

    // 应用阈值和边缘锐化
    float Edge = max(_CloudEdgeSharpness, _CloudDensityThreshold + 0.001);
    float Edge2 = max(_MixEdgeFieldFactor, _CloudDensityThreshold + 0.001);
    float erosion = smoothstep(_CloudDensityThreshold, Edge2, Density);
    Density = smoothstep(_CloudDensityThreshold, Edge, Density * HeightFactor);

    // 细节噪声
    float detailFactor = NoiseDetail.r * saturate(1.0 - NoiseDetail.b + 0.0);
    float detailEdge = max(_CloudDetailSharpness, _CloudDetailThreshold + 0.001);
    detailFactor = smoothstep(_CloudDetailThreshold, detailEdge, detailFactor);
    detailFactor = lerp(1.0, detailFactor, _CloudDetailMix);
    detailFactor = lerp(detailFactor, 1.0, _OnlyOnEdgeErosion * erosion);

    // 最终密度
    return Density * lerp(1.0, detailFactor, sunHeight) * sunHeight * _CloudDensityMultiplier;
}
```

---

## 4. 水体渲染子系统详解

### 4.1 平面反射系统

#### 4.1.1 实现原理

平面反射系统通过创建一个反射相机，模拟光线在水面上的反射效果：

- **对称相机位置**：将相机位置沿水面高度对称
- **对称相机旋转**：将相机的俯仰角取反
- **斜截投影矩阵**：确保反射图像正确对齐
- **背面剔除**：处理反射物体的可见性

#### 4.1.2 核心代码

**PlanarReflectionRF.cs**：

```csharp
private void UpdateReflectionCamera(Camera mainCamera)
{
    if (_reflectionCamera == null) return;

    _reflectionCamera.CopyFrom(mainCamera);
    _reflectionCamera.useOcclusionCulling = false;
    _reflectionCamera.cullingMask = m_Settings.reflectLayers;
    _reflectionCamera.depth = -1000;
    _reflectionCamera.rect = new Rect(0, 0, 1, 1);

    // 水面位置
    float waterHeight = m_Settings.waterHeight + m_Settings.planeOffset;

    // 1. 计算反射相机位置（对称 Y 轴）
    Vector3 camPos = mainCamera.transform.position;
    float reflectedY = waterHeight - (camPos.y - waterHeight);
    Vector3 reflectedPos = new Vector3(camPos.x, reflectedY, camPos.z);
    _reflectionCamera.transform.position = reflectedPos;

    // 2. 用欧拉角计算反射相机旋转（方位角不变，俯仰角取反）
    Vector3 euler = mainCamera.transform.eulerAngles;
    float reflectedPitch = -euler.x;
    if (reflectedPitch > 90) reflectedPitch -= 360;
    if (reflectedPitch < -90) reflectedPitch += 360;
    _reflectionCamera.transform.rotation = Quaternion.Euler(reflectedPitch, euler.y, 0);

    // 3. 设置反射相机的 view matrix
    _reflectionCamera.worldToCameraMatrix = _reflectionCamera.worldToCameraMatrix;

    // 4. 计算斜截投影矩阵
    Vector4 clipPlane = CameraSpacePlane(_reflectionCamera,
        new Vector3(0, waterHeight, 0),
        Vector3.up, 1.0f, m_Settings.clipPlaneOffset);

    _reflectionCamera.projectionMatrix = mainCamera.CalculateObliqueMatrix(clipPlane);
}
```

### 4.2 屏幕MipMap处理系统

#### 4.2.1 实现原理

屏幕MipMap处理系统通过生成带有Mipmap层级的降采样屏幕纹理，实现水的散射模糊效果和焦散效果：

- **降采样**：减少纹理分辨率，提高性能
- **Mipmap生成**：创建不同分辨率的纹理层级
- **预处理**：在生成Mipmap前对纹理进行自定义处理，如焦散效果
- **基于水深选择Mipmap层级**：实现水的深度模糊效果

#### 4.2.2 核心代码

**ScreenMipMapPass.cs**：

```csharp
public override void Execute(ScriptableRenderContext context, ref RenderingData renderingData)
{
    if (screenMipMapRT == null || tempDepthRT == null)
        return;
    
    CommandBuffer cmd = CommandBufferPool.Get("ScreenMipMap");

    RenderTargetIdentifier cameraTarget = renderingData.cameraData.renderer.cameraColorTargetHandle;
    
    using (new ProfilingScope(cmd, new ProfilingSampler("ScreenMipMap Color")))
    {
        cmd.SetRenderTarget(screenMipMapRT, tempDepthRT);
        cmd.ClearRenderTarget(true, true, Color.black);
        
        context.ExecuteCommandBuffer(cmd);
        cmd.Clear();
        
        DrawingSettings drawingSettings = CreateDrawingSettings(shaderTagIds, ref renderingData, 
            renderingData.cameraData.defaultOpaqueSortFlags);
        
        FilteringSettings filteringSettings = new FilteringSettings(RenderQueueRange.opaque);
        
        context.DrawRenderers(renderingData.cullResults, ref drawingSettings, ref filteringSettings);
    }
    
    context.ExecuteCommandBuffer(cmd);
    cmd.Clear();
    
    if (enablePreProcess && preProcessMaterial != null && tempColorRT != null)
    {
        using (new ProfilingScope(cmd, new ProfilingSampler("ScreenMipMap PreProcess")))
        {
            preProcessMaterial.SetTexture("_MainTex", screenMipMapRT);
            preProcessMaterial.SetTexture("_CameraDepthTexture", tempDepthRT);
            cmd.Blit(screenMipMapRT, tempColorRT, preProcessMaterial, 0);
            cmd.Blit(tempColorRT, screenMipMapRT, preProcessMaterial, 1);
        }
        context.ExecuteCommandBuffer(cmd);
        cmd.Clear();
    }
    
    using (new ProfilingScope(cmd, new ProfilingSampler("ScreenMipMap GenerateMips")))
    {
        cmd.GenerateMips(screenMipMapRT);
        cmd.SetGlobalTexture("_ScreenMipMapRT", screenMipMapRT);
    }
    
    context.ExecuteCommandBuffer(cmd);
    CommandBufferPool.Release(cmd);
}
```

### 4.3 吃水线系统

#### 4.3.1 实现原理

吃水线系统通过计算水面高度图和摄像机近平面世界位置，生成吃水线和水下效果：

- **水面高度图**：使用俯视正交相机渲染场景物体的高度
- **摄像机近平面世界位置**：通过屏幕UV进行双线性插值获取
- **高度对比**：将近平面世界位置与水面高度图进行对比，生成吃水线Mask
- **多帧混合**：使用双缓冲实现上一帧结果的持续效果

#### 4.3.2 核心代码

**WaterLineRF.cs**：

```csharp
// 计算摄像机近平面上的四个点
float nearOffset = 0.0f;
Vector3 nTL = camera.ViewportToWorldPoint(new Vector3(0, 1, camera.nearClipPlane + nearOffset));
Vector3 nTR = camera.ViewportToWorldPoint(new Vector3(1, 1, camera.nearClipPlane + nearOffset));
Vector3 nBL = camera.ViewportToWorldPoint(new Vector3(0, 0, camera.nearClipPlane + nearOffset));
Vector3 nBR = camera.ViewportToWorldPoint(new Vector3(1, 0, camera.nearClipPlane + nearOffset));
material.SetVector("_NearPlaneCornersTL", nTL);
material.SetVector("_NearPlaneCornersTR", nTR);
material.SetVector("_NearPlaneCornersBL", nBL);
material.SetVector("_NearPlaneCornersBR", nBR);

// 生成吃水线以及水下的部分Mask
material.SetTexture("_waterLineMaskRT_Read", _waterLineMaskRT_Read);
cmd.Blit(_waterLineMaskRT_Read, _waterLineMaskRT.nameID, material, 1);
cmd.Blit(_waterLineMaskRT, _waterLineMaskRT_Read);
material.SetTexture("_WaterLineMaskRT", _waterLineMaskRT);
```

### 4.4 浅水方程系统

#### 4.4.1 实现原理

浅水方程系统使用Compute Shader实现水面涟漪效果：

- **流体动力学方程**：基于浅水方程模拟水面波动
- **高度缓冲区**：使用三个缓冲区（环形缓冲）存储当前、上一帧和上上一帧的水面高度
- **扰动机制**：通过目标物体的移动产生水面扰动

#### 4.4.2 核心代码

**WaveEquationControl.cs**：

```csharp
void Update()
{
    Vector2 uv = Vector2.zero;

    // 世界坐标转UV（假设水面是10x10单位）
    Vector3 pos = target.position;
    uv = new Vector2(
        (transform.position.x - pos.x) / 10f + 0.5f,
        (transform.position.z - pos.z) / 10f + 0.5f
    );

    if (target != null)
    {
        movementDelta = target.position - lastTargetPos;
    }
    
    if (target != null && followTarget)
    {
        transform.position = new Vector3(
            target.position.x,
            followYAxis ? target.position.y : transform.position.y,
            target.position.z
        );
    }
    
    // 仅仅当目标存在且位置发生变化时才增加扰动
    float finalStrength = movementDelta.magnitude > 0.001f ? strength : 0f;
    lastTargetPos = target.position;

    // 设置参数
    computeShader.SetFloat("texSize", textureSize);
    computeShader.SetFloat("dt", Time.deltaTime);
    computeShader.SetFloat("alpha", alpha);
    computeShader.SetFloat("beta", beta);
    computeShader.SetFloat("damping", damping);
    computeShader.SetFloat("strength", target != null ? finalStrength : 0f);
    computeShader.SetFloat("radius", radius);
    computeShader.SetVector("sourceUV", uv);
    computeShader.SetVector("movementDelta", followTarget ? movementDelta / 10.0f : Vector2.zero);

    // 设置纹理
    computeShader.SetTexture(simKernel, "CurrHeight", heightBuffers[0].rt);
    computeShader.SetTexture(simKernel, "PrevHeight", heightBuffers[1].rt);
    computeShader.SetTexture(simKernel, "PrevPrevHeight", heightBuffers[2].rt);
    
    // 调度
    int groups = Mathf.CeilToInt(textureSize / 8f);
    computeShader.Dispatch(simKernel, groups, groups, 1);
    
    // 交换缓冲区（环形缓冲）
    RotateBuffers();
    
    // 调试
    if (debugMaterial != null)
    {
        debugMaterial.SetTexture("_MainTex", heightBuffers[0].rt);
    }
    Graphics.Blit(heightBuffers[0].rt, debugRT);
}

void RotateBuffers()
{
    var temp = heightBuffers[2];
    heightBuffers[2] = heightBuffers[1];
    heightBuffers[1] = heightBuffers[0];
    heightBuffers[0] = temp;
}
```

---

## 5. 系统交互关系

### 5.1 纹理流

```
┌─────────────────────────┐     ┌─────────────────────────┐
│ 大气散射全景图生成器     │────>│ 体积云渲染系统         │
└─────────────────────────┘     └─────────────────────────┘
        │                              │
        ▼                              ▼
┌─────────────────────────┐     ┌─────────────────────────┐
│ 平面反射系统           │────>│ 水面Shader             │
└─────────────────────────┘     └─────────────────────────┘
        │                              │
        ▼                              ▼
┌─────────────────────────┐     ┌─────────────────────────┐
│ 屏幕MipMap处理系统    │────>│ 水下折射与焦散         │
└─────────────────────────┘     └─────────────────────────┘
        │                              │
        ▼                              ▼
┌─────────────────────────┐     ┌─────────────────────────┐
│ 吃水线系统             │────>│ 水面与物体交互         │
└─────────────────────────┘     └─────────────────────────┘
        │                              │
        ▼                              ▼
┌─────────────────────────┐     ┌─────────────────────────┐
│ 浅水方程系统           │────>│ 水面涟漪与扰动         │
└─────────────────────────┘     └─────────────────────────┘
```

### 5.2 渲染流程

1. **大气散射全景图生成**：在场景初始化或参数变化时，使用Compute Shader计算大气散射效果并生成全景图
2. **体积云渲染**：在每帧渲染中，使用大气散射图作为环境纹理，渲染体积云
3. **平面反射渲染**：在不透明物体渲染后，生成平面反射纹理
4. **屏幕MipMap处理**：对屏幕纹理进行降采样和Mipmap生成，处理焦散效果
5. **吃水线计算**：计算水面高度图和吃水线Mask
6. **浅水方程更新**：更新水面涟漪纹理
7. **水面渲染**：使用上述所有纹理和数据，渲染最终的水面效果

---

## 6. 技术实现细节

### 6.1 性能优化

- **降采样**：平面反射和屏幕MipMap处理使用降采样，减少计算量
- **光线步进优化**：体积云和大气散射计算中使用自适应步长和提前终止
- **多线程计算**：使用Compute Shader进行并行计算，提高性能
- **缓冲区管理**：合理管理渲染纹理，避免内存泄漏
- **指数步进**：光线步进使用指数分布，近密远疏，减少采样数

### 6.2 视觉效果增强

- **多重散射**：模拟光在云中的多次散射，增强真实感
- **丁达尔效应**：模拟光线穿过云层时的散射效果
- **焦散效果**：通过屏幕MipMap处理生成水下焦散
- **吃水线效果**：增强水体与场景物体的交互感
- **动态水面**：通过浅水方程模拟水面涟漪
- **SSS次表面散射**：模拟光线在水体内部的散射
- **太阳闪烁**：模拟阳光在水面的闪烁效果

### 6.3 系统集成

- **模块化设计**：各个子系统相对独立，便于维护和扩展
- **统一的纹理管理**：通过全局纹理变量在不同系统间传递数据
- **渲染管线集成**：使用URP的ScriptableRendererFeature系统，无缝集成到渲染管线中

---

## 7. 总结

本项目实现了一个高度集成的水体大气渲染系统，通过多个子系统的协同工作，实现了高质量的实时水体和大气效果。系统采用了多种先进的渲染技术，包括光线步进、体积云渲染、平面反射、屏幕空间效果等，为游戏和虚拟现实应用提供了逼真的自然环境效果。

### 7.1 核心技术亮点

- **实时大气散射**：使用Compute Shader实时计算大气散射效果
- **体积云渲染**：实现上下半球对称的体积云，避免额外的反射纹理计算
- **水体BSDF**：采用双通道BSDF模型，实现逼真的水体散射效果
- **光线步进**：使用指数步进算法，高效计算水体体积散射
- **平面反射**：通过相机矩阵变换实现准确的平面反射
- **屏幕空间效果**：利用Mipmap和屏幕空间计算实现水的散射和焦散
- **浅水方程**：使用流体动力学方程模拟水面涟漪
- **吃水线效果**：增强水体与场景物体的交互感

### 7.2 应用场景

- **开放世界游戏**：提供逼真的天空和水体效果
- **虚拟现实应用**：增强环境沉浸感
- **景观可视化**：展示自然环境的美丽效果
- **教育模拟**：模拟大气和水体的物理特性

本项目展示了如何通过现代GPU渲染技术，在实时应用中实现高质量的自然环境效果，为游戏和图形应用开发提供了有价值的参考。